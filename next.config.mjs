// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Local-only images (no remote domains)
  images: {
    // leave empty => sirf /public wali images allow hongi
  },
};

export default nextConfig;
